---
phase: 03-code-quality
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - MDEValidator/MDEValidator.psd1
autonomous: true
requirements: [QUAL-01, QUAL-02]

must_haves:
  truths:
    - "MDEValidator.psd1 LicenseUri is set to a non-empty valid URL (not an empty string)"
    - "MDEValidator.psd1 ProjectUri is set to a non-empty valid URL (not an empty string)"
    - "Test-ModuleManifest -Path ./MDEValidator/MDEValidator.psd1 completes without errors"
    - "Invoke-ScriptAnalyzer with settings file returns 0 violations (full QUAL-01 acceptance gate)"
  artifacts:
    - path: "MDEValidator/MDEValidator.psd1"
      provides: "Publish-ready manifest with valid LicenseUri and ProjectUri"
      contains: "https://github.com/mdavis-xyz/MDEValidator"
  key_links:
    - from: "MDEValidator/MDEValidator.psd1"
      to: "https://github.com/mdavis-xyz/MDEValidator"
      via: "PSData.ProjectUri field in PrivateData block"
      pattern: "ProjectUri.*github\\.com"
---

<objective>
Update the module manifest with the two missing URI fields (LicenseUri and ProjectUri), then run the final acceptance gate confirming all three Phase 3 requirements are met.

Purpose: PSGallery rejects manifests with empty-string URI fields. The manifest currently has `LicenseUri = ''` and `ProjectUri = ''`. Replacing them with real URLs satisfies QUAL-02. The verification task then confirms QUAL-01 (zero PSSA violations) and QUAL-03 (settings file present) as the phase completion gate.

Output:
- Updated `MDEValidator/MDEValidator.psd1` with placeholder GitHub URLs for LicenseUri and ProjectUri
- Verified PSSA clean run and manifest validity confirming all QUAL requirements
</objective>

<execution_context>
@./.planning/phases/03-code-quality/03-CONTEXT.md
@./MDEValidator/MDEValidator.psd1
@./.PSScriptAnalyzerSettings.psd1
</execution_context>

<context>
## Phase Goal
Module passes static analysis and has complete, publish-ready manifest metadata.

## Key Requirements
- **QUAL-01**: PSScriptAnalyzer reports zero errors and zero warnings — verified here as the acceptance gate
- **QUAL-02**: Module manifest includes LicenseUri, ProjectUri, Tags, and ReleaseNotes

## Pre-conditions (from Plan 03-01)
- `.PSScriptAnalyzerSettings.psd1` exists at repo root
- All 5 code violations are fixed (3 empty catch blocks, 1 unused variable, 1 unused parameter)
- PSSA already passes at 0 violations as of 03-01 completion

## Manifest Current State
- `LicenseUri = ''` — empty string, must be replaced (PSGallery rejects empty-string URIs)
- `ProjectUri = ''` — empty string, must be replaced
- `Tags = @('Security','Defender','MDE','Endpoint','Validation','Windows')` — already set, no change needed
- `ReleaseNotes = 'Initial release of MDEValidator module.'` — already set, no change needed

## Locked Decision
Use placeholder GitHub URLs:
- `ProjectUri = 'https://github.com/mdavis-xyz/MDEValidator'`
- `LicenseUri = 'https://github.com/mdavis-xyz/MDEValidator/blob/main/LICENSE'`

These are placeholders. The actual URLs must be verified/updated before PSGallery publish.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MDEValidator.psd1 with valid LicenseUri and ProjectUri</name>
  <files>MDEValidator/MDEValidator.psd1</files>
  <action>
Read `MDEValidator/MDEValidator.psd1` to locate the `PSData` block inside `PrivateData`. The current manifest ends with:

```powershell
    PrivateData = @{
        PSData = @{
            # Tags applied to this module
            Tags = @('Security', 'Defender', 'MDE', 'Endpoint', 'Validation', 'Windows')

            # A URL to the license for this module
            LicenseUri = ''

            # A URL to the main website for this project
            ProjectUri = ''

            # ReleaseNotes of this module
            ReleaseNotes = 'Initial release of MDEValidator module.'
        }
    }
```

Replace the two empty-string URI values with the placeholder GitHub URLs:

```powershell
    PrivateData = @{
        PSData = @{
            # Tags applied to this module
            Tags = @('Security', 'Defender', 'MDE', 'Endpoint', 'Validation', 'Windows')

            # A URL to the license for this module
            LicenseUri = 'https://github.com/mdavis-xyz/MDEValidator/blob/main/LICENSE'

            # A URL to the main website for this project
            ProjectUri = 'https://github.com/mdavis-xyz/MDEValidator'

            # ReleaseNotes of this module
            ReleaseNotes = 'Initial release of MDEValidator module.'
        }
    }
```

**Do NOT change any other fields** in the manifest (FunctionsToExport, ModuleVersion, GUID, Author, Description, PowerShellVersion, etc.). This is a surgical two-line change.
  </action>
  <verify>
    <automated>
$raw = Get-Content 'MDEValidator/MDEValidator.psd1' -Raw
if ($raw -match "LicenseUri\s*=\s*''") { throw "FAIL: LicenseUri is still an empty string" }
if ($raw -match "ProjectUri\s*=\s*''") { throw "FAIL: ProjectUri is still an empty string" }
if ($raw -notmatch 'github\.com/mdavis-xyz/MDEValidator') { throw "FAIL: GitHub URLs not found in manifest" }

$result = Test-ModuleManifest -Path 'MDEValidator/MDEValidator.psd1' -ErrorAction Stop
Write-Host "PASS: Test-ModuleManifest succeeded. Module: $($result.Name) v$($result.Version)" -ForegroundColor Green
    </automated>
  </verify>
  <done>MDEValidator.psd1 has non-empty LicenseUri and ProjectUri set to GitHub placeholder URLs. Test-ModuleManifest passes without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Final Phase 3 verification gate (QUAL-01 + QUAL-02 + QUAL-03)</name>
  <files></files>
  <action>
Run the full acceptance checks for all three Phase 3 requirements. No files are modified — this task only runs verification commands and reports results. A failure in any check means Phase 3 is not complete.

## Check 1 — QUAL-03: Settings file exists at repo root
Verify `.PSScriptAnalyzerSettings.psd1` exists.

## Check 2 — QUAL-01: Zero PSSA violations against full module
Run Invoke-ScriptAnalyzer against `.\MDEValidator` with the settings file and `-Severity @('Error', 'Warning')`. Expected: no results returned. If any violations are returned, print them and fail.

## Check 3 — QUAL-02: Manifest metadata complete
Use `Import-PowerShellDataFile` to parse the manifest and verify all four required PSData fields have non-empty values:
- `Tags` — non-empty array
- `LicenseUri` — non-empty, non-whitespace string
- `ProjectUri` — non-empty, non-whitespace string
- `ReleaseNotes` — non-empty string
  </action>
  <verify>
    <automated>
# QUAL-03: Settings file exists
if (-not (Test-Path '.PSScriptAnalyzerSettings.psd1')) {
    throw "FAIL QUAL-03: .PSScriptAnalyzerSettings.psd1 not found at repo root"
}
Write-Host "PASS QUAL-03: Settings file exists" -ForegroundColor Green

# QUAL-01: Zero PSSA violations
$violations = Invoke-ScriptAnalyzer `
    -Path .\MDEValidator `
    -Recurse `
    -Settings .\.PSScriptAnalyzerSettings.psd1 `
    -Severity @('Error', 'Warning')
if ($violations) {
    $violations | Format-Table ScriptName, Line, RuleName, Message -AutoSize
    throw "FAIL QUAL-01: $($violations.Count) violation(s) found — Phase 3 not complete"
}
Write-Host "PASS QUAL-01: PSScriptAnalyzer 0 violations" -ForegroundColor Green

# QUAL-02: Manifest metadata
$manifest = Import-PowerShellDataFile 'MDEValidator/MDEValidator.psd1'
$psData = $manifest.PrivateData.PSData
if (-not $psData.Tags -or $psData.Tags.Count -eq 0) { throw "FAIL QUAL-02: Tags field is missing or empty" }
if ([string]::IsNullOrWhiteSpace($psData.LicenseUri)) { throw "FAIL QUAL-02: LicenseUri is empty or whitespace" }
if ([string]::IsNullOrWhiteSpace($psData.ProjectUri)) { throw "FAIL QUAL-02: ProjectUri is empty or whitespace" }
if ([string]::IsNullOrWhiteSpace($psData.ReleaseNotes)) { throw "FAIL QUAL-02: ReleaseNotes is empty or whitespace" }
Write-Host "PASS QUAL-02: Manifest metadata complete (Tags=$($psData.Tags.Count) items, LicenseUri and ProjectUri set)" -ForegroundColor Green

Write-Host "" 
Write-Host "ALL PHASE 3 REQUIREMENTS MET (QUAL-01, QUAL-02, QUAL-03)" -ForegroundColor Cyan
    </automated>
  </verify>
  <done>All three QUAL requirements verified: QUAL-01 (0 PSSA violations), QUAL-02 (manifest LicenseUri, ProjectUri, Tags, ReleaseNotes all non-empty), QUAL-03 (settings file present). Phase 3 complete.</done>
</task>

</tasks>

<verification>
After both tasks:
- `Test-ModuleManifest -Path .\MDEValidator\MDEValidator.psd1` succeeds without errors
- `Invoke-ScriptAnalyzer -Path .\MDEValidator -Recurse -Settings .\.PSScriptAnalyzerSettings.psd1` returns 0 results
- `Import-PowerShellDataFile MDEValidator/MDEValidator.psd1` shows non-empty LicenseUri, ProjectUri, Tags, and ReleaseNotes
</verification>

<success_criteria>
Test-ModuleManifest passes. PSSA reports 0 violations with settings file. All four PSData metadata fields contain real non-empty values. All QUAL-01, QUAL-02, and QUAL-03 requirements confirmed satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-quality/03-02-SUMMARY.md` with:
- Manifest fields updated (LicenseUri, ProjectUri before/after values)
- Final PSSA result (must be 0 violations)
- Confirmation all QUAL-01, QUAL-02, QUAL-03 requirements met
</output>
