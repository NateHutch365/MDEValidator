---
phase: 04-cicd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [CICD-01, CICD-02, CICD-03, CICD-05]

must_haves:
  truths:
    - "ci.yml triggers on both push to main AND pull_request to main"
    - "Job runs on windows-latest runner"
    - "Pester 5.7.1+ is explicitly installed before tests run"
    - "PSScriptAnalyzer is explicitly installed before lint runs"
    - "run-tests.ps1 is invoked and its non-zero exit code fails the job"
    - "coverage.xml and test-results.xml are uploaded as artifacts on every run (including failures)"
    - "JaCoCo PR comment step is gated on pull_request event only"
    - "PSScriptAnalyzer step throws on any Error or Warning violation"
    - "PSScriptAnalyzer runs AFTER artifact upload steps so coverage is preserved even when lint fails"
    - "Local test suite passes (exit 0) before committing — green baseline confirmed"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow for test + lint + coverage"
      contains: "windows-latest"
      exports: []
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "run-tests.ps1"
      via: "pwsh run step"
      pattern: "run-tests\\.ps1"
    - from: ".github/workflows/ci.yml"
      to: ".PSScriptAnalyzerSettings.psd1"
      via: "-Settings parameter in Invoke-ScriptAnalyzer"
      pattern: "PSScriptAnalyzerSettings"
    - from: ".github/workflows/ci.yml"
      to: "Tests/Artifacts/coverage.xml"
      via: "actions/upload-artifact@v4 path"
      pattern: "coverage\\.xml"
    - from: ".github/workflows/ci.yml"
      to: "secrets.GITHUB_TOKEN"
      via: "madrapps/jacoco-report token param"
      pattern: "GITHUB_TOKEN"
---

<objective>
Create the primary CI workflow that runs on every push and PR to `main`, executing the full Pester test suite, uploading coverage artifacts, posting JaCoCo PR comments, and failing the build on any PSScriptAnalyzer violation.

Purpose: Enforce automated quality gates so no broken code or lint violations reach `main` undetected.
Output: `.github/workflows/ci.yml` — a complete, runnable GitHub Actions workflow.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cicd/04-RESEARCH.md
@run-tests.ps1
</context>

<interfaces>
<!-- Key facts the executor needs — no codebase exploration required. -->

run-tests.ps1 invocation and outputs:
- Invoke via: `.\run-tests.ps1` (pwsh, from repo root)
- Exits non-zero if any Pester tests fail (`exit $result.FailedCount`)
- Produces: `Tests/Artifacts/coverage.xml` (JaCoCo format)
- Produces: `Tests/Artifacts/test-results.xml` (NUnitXml format)

PSScriptAnalyzer settings:
- Settings file: `.PSScriptAnalyzerSettings.psd1` (repo root, committed in Phase 3)
- Target path: `.\MDEValidator` (NOT repo root — avoids flagging test helpers)
- Severity filter: `@('Error', 'Warning')`

Workflow action versions (pinned, current March 2026):
- `actions/checkout@v4`
- `actions/upload-artifact@v4`
- `madrapps/jacoco-report@v1.7.2`

JaCoCo action requirements:
- Needs `pull-requests: write` in job permissions to post PR comment
- Only runs on `github.event_name == 'pull_request'` (not push-to-main events)
- `min-coverage-overall: 60` — fails the step if overall coverage drops below 60%
- `update-comment: true` — edits existing comment rather than creating duplicates on re-runs

PSScriptAnalyzer failure mechanism:
- Use `throw` inside `pwsh` step (NOT `Write-Error` alone)
- GitHub Actions sets `$ErrorActionPreference = 'Stop'` automatically for pwsh steps
- `throw` terminates the script with a non-zero exit code → fails the step → fails the job
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml` with the EXACT content below. Create the `.github/workflows/` directory if it does not exist.

IMPORTANT ordering: PSScriptAnalyzer runs LAST (after artifact uploads). This ensures coverage.xml and test-results.xml are always uploaded even when lint fails. The `if: always()` guards on artifact steps ensure upload even when Pester fails.

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-and-lint:
    name: Test and Lint
    runs-on: windows-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Pester
        shell: pwsh
        run: Install-Module -Name Pester -MinimumVersion 5.7.1 -Force -Scope CurrentUser

      - name: Install PSScriptAnalyzer
        shell: pwsh
        run: Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser

      - name: Run Pester Tests
        shell: pwsh
        run: .\run-tests.ps1

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: Tests/Artifacts/test-results.xml

      - name: Upload Coverage Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: Tests/Artifacts/coverage.xml

      - name: JaCoCo Coverage Report on PR
        if: github.event_name == 'pull_request'
        uses: madrapps/jacoco-report@v1.7.2
        with:
          paths: ${{ github.workspace }}/Tests/Artifacts/coverage.xml
          token: ${{ secrets.GITHUB_TOKEN }}
          min-coverage-overall: 60
          title: Code Coverage Report
          update-comment: true

      - name: Run PSScriptAnalyzer
        shell: pwsh
        run: |
          $violations = Invoke-ScriptAnalyzer `
            -Path .\MDEValidator `
            -Recurse `
            -Settings .\.PSScriptAnalyzerSettings.psd1 `
            -Severity @('Error', 'Warning')

          if ($violations) {
            $violations | Format-Table ScriptName, Line, RuleName, Message -AutoSize
            throw "PSScriptAnalyzer found $($violations.Count) violation(s). Build failed."
          }

          Write-Host "PSScriptAnalyzer: PASS (0 violations)" -ForegroundColor Green
```
  </action>
  <verify>
    <automated>
pwsh -NoProfile -Command "
  if (-not (Test-Path '.github/workflows/ci.yml')) { throw 'ci.yml missing' }
  $content = Get-Content '.github/workflows/ci.yml' -Raw
  @('windows-latest','madrapps/jacoco-report@v1.7.2','PSScriptAnalyzerSettings','run-tests.ps1','upload-artifact@v4','pull-requests: write') | ForEach-Object {
    if ($content -notmatch [regex]::Escape($_)) { throw \"Missing required pattern: $_\" }
  }
  Write-Host 'ci.yml structure verified' -ForegroundColor Green
"
    </automated>
  </verify>
  <done>`.github/workflows/ci.yml` exists with all required steps: checkout, install Pester 5.7.1+, install PSScriptAnalyzer, run-tests.ps1, upload artifacts (if: always()), JaCoCo PR comment (pull_request only), PSScriptAnalyzer lint with throw on violations.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test CI prerequisites locally</name>
  <files>(none — read-only verification)</files>
  <action>
Run the local test suite and PSScriptAnalyzer to confirm the CI will pass on its first run. These commands replicate exactly what ci.yml will execute on the runner.

Step 1 — Run Pester tests (mirrors the "Run Pester Tests" CI step):
```powershell
pwsh -NoProfile -File ./run-tests.ps1
```
Expected: exits with code 0 (all tests pass). If non-zero, investigate failing tests before continuing — CI will fail immediately on first push.

Step 2 — Run PSScriptAnalyzer (mirrors the "Run PSScriptAnalyzer" CI step):
```powershell
pwsh -NoProfile -Command "
  \$violations = Invoke-ScriptAnalyzer \`
    -Path .\MDEValidator \`
    -Recurse \`
    -Settings .\.PSScriptAnalyzerSettings.psd1 \`
    -Severity @('Error', 'Warning')
  if (\$violations) { throw \"PSSA: \$(\$violations.Count) violation(s)\" }
  Write-Host 'PSScriptAnalyzer: PASS' -ForegroundColor Green
"
```
Expected: exits with code 0 (zero violations). Phase 3 ensured this is clean — confirm it is still clean.

Do NOT continue if either check fails. Fix root cause first.
  </action>
  <verify>
    <automated>
pwsh -NoProfile -File ./run-tests.ps1; if ($LASTEXITCODE -ne 0) { throw "Test suite failed with exit code $LASTEXITCODE" } else { Write-Host "Test suite PASSED" -ForegroundColor Green }
    </automated>
  </verify>
  <done>Both `run-tests.ps1` and `Invoke-ScriptAnalyzer` exit with code 0 locally, confirming the CI workflow will pass on its first trigger.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `.github/workflows/ci.yml` exists
- File contains all 8 required steps in correct order
- Local test suite passes (exit 0)
- Local PSSA check passes (0 violations)
</verification>

<success_criteria>
CICD-01: ci.yml triggers on push AND pull_request to main ✓
CICD-02: PSScriptAnalyzer runs and throws on violations ✓
CICD-03: windows-latest runner specified ✓
CICD-05: coverage.xml uploaded as artifact; JaCoCo PR comment on pull_request events ✓
Local baseline confirmed green — CI will pass on first push ✓
</success_criteria>

<output>
After completion, create `.planning/phases/04-cicd/04-01-SUMMARY.md`
</output>
